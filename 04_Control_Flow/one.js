// if

const isUserLoggedIn = true;
if (3 != 2) {
  console.log("Execuated");
}

// 2 === "2"
//

//<, >, <=, >=, ==, !=, === !==

const score = 200;

if (score > 100) {
  const power = "fly";
  console.log(`User Power : ${power}`);
}
