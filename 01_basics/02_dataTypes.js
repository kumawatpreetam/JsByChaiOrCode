"use strict"; //treat all js code as a newer version but at present it default treat as a newer version 
//Code readablity is always must

//original docs of js is https://tc39.es/ecma262/ 


let name = "Preetam"; //String DataTypes

let age = 21; //Number

let isLogin = true; //Boolean
console.log(typeof (isLogin));


let state;

// null = standalone value and it is a object

//undefine :

//typeof : It find type of the data 

